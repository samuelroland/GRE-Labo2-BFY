package gre.lab2.graph;

/**
 * Useless interface to force students to comply with the method signature.
 */
public interface IBellmanFordYensAlgorithm {
  BFYResult compute(WeightedDigraph graph, int from);
}
